# OnsetLab

Platform to build customized agents that run using a fine-tuned SLM with access to MCP servers according to your particular use case.

"Automated agent builder: problem statement → fine-tuned SLM + MCP tools → deployable agent"